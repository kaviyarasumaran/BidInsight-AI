from __future__ import annotations


async def extract_evidence(*, combined_text: str) -> list[dict]:
    # Placeholder: call an LLM + rule-based extractors for vendor evidence objects.
    return []
